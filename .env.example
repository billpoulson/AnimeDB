# Video output format (mkv, mp4, etc.)
OUTPUT_FORMAT=mkv

# Build SHA (set automatically by install scripts, do not edit manually)
# BUILD_SHA=

# Network mode: "bridge" (default, works everywhere) or "host" (Linux only, required for UPnP)
# NETWORK_MODE=host

# External URL override for federation (required on Windows/Mac where UPnP cannot work)
# EXTERNAL_URL=http://your-public-ip:3000

# Plex integration (optional — leave empty to skip)
# Use "Link with Plex" in Settings for OAuth, or set manually:
PLEX_URL=http://your-plex-server:32400
PLEX_TOKEN=your-plex-token
PLEX_SECTION_MOVIES=1
PLEX_SECTION_TV=2
# Optional: stable client ID for Plex OAuth (default: auto-generated per instance)
# PLEX_CLIENT_ID=
