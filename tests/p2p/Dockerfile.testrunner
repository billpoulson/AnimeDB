FROM node:22-slim

RUN apt-get update && \
    apt-get install -y --no-install-recommends curl jq && \
    rm -rf /var/lib/apt/lists/*

WORKDIR /test
COPY tests/p2p/run.sh ./run.sh
RUN chmod +x ./run.sh

CMD ["./run.sh"]
